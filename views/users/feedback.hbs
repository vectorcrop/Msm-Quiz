<div class="page-content-wrapper py-3">
    <div class="container">
        <!-- User Information-->

        <!-- User Meta Data-->


        {{!-- <div class="card user-data-card mb-3">
            <div class="card-body">
                <h5 class="mt-2 mb-2">Login Details</h5>
                <form action="#">
                    <div class="form-group mb-3">
                        <label class="form-label" for="Username">Username</label>
                        <input class="form-control" id="Username" value="{{user.Fname}}{{user.Lname}}" type="text"
                            value="@designing-world" placeholder="First Name" readonly>
                    </div>

                    <div class="form-group mb-3">
                        <label class="form-label" for="email">Phone Number</label>
                        <input class="form-control" id="email" type="text" value="{{user.Mobile}}" placeholder="Mobile"
                            readonly="">
                    </div>

                </form>
            </div>
        </div> --}}

        <div class="card user-data-card mt-4">
            <div class="card-body">
                <form action="/add-feedback" method="POST">
                    {{#if user}}
                    <input type="hidden" name="name" value="{{user.Fname}} {{user.Lname}}">
                    <input type="hidden" name="phone" value="{{user.Mobile}}">
                    <input type="hidden" name="district" value="{{user.district}}">
                    <input type="hidden" name="zone" value="{{user.zone}}">
                    <input type="hidden" name="type" value="{{user.type}}">
                    <input type="hidden" name="date" id="datetime">



                    <div class="form-group mb-3">
                        <label class="form-label" for="bio">Your Feedback</label>
                        <textarea class="form-control" id="bio" name="feedback" cols="30" rows="10"
                            placeholder="Drop your feedback"></textarea>
                    </div>
                    <button class="btn btn-primary">Submit</button>
                    {{/if}}
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    // Get current date and time
    const now = new Date();
    const year = now.getFullYear();
    const month = (now.getMonth() + 1).toString().padStart(2, '0');
    const day = now.getDate().toString().padStart(2, '0');
    const hours = ((now.getHours() + 11) % 12 + 1).toString().padStart(2, '0'); // Convert to 12-hour format
    const minutes = now.getMinutes().toString().padStart(2, '0');
    const ampm = now.getHours() >= 12 ? 'PM' : 'AM';

    // Set default value to current date and time in MM/DD/YYYY format
    const defaultDateTime = `${month}/${day}/${year}T${hours}:${minutes}`;
    document.getElementById('datetime').value = defaultDateTime;
</script>